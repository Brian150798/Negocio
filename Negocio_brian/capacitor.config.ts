import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Negocio_brian',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
